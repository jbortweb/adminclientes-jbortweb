const config = {
  theme:'genesis',
  messages: {
    en: {
      // No definir mensajes globales aquí desactivará los mensajes de error predeterminados
      validation: {
        // required: '' // Puedes dejar esto vacío o no definirlo para desactivar mensajes globales
      }
    }
  }
}

export default config